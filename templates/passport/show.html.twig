{% extends 'base.html.twig' %}

{% block title %}Passport{% endblock %}

{% block body %}
    <h1>Passport</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ passport.id }}</td>
            </tr>
            <tr>
                <th>Passnum</th>
                <td>{{ passport.passnum }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_passport_index') }}">back to list</a>

    <a href="{{ path('app_passport_edit', {'id': passport.id}) }}">edit</a>

    {{ include('passport/_delete_form.html.twig') }}
{% endblock %}
