{% extends 'base.html.twig' %}

{% block title %}Emploee{% endblock %}

{% block body %}
    <h1>Emploee</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ emploee.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ emploee.name }}</td>
            </tr>
            <tr>
                <th>Fathername</th>
                <td>{{ emploee.fathername }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_emploee_index') }}">back to list</a>

    <a href="{{ path('app_emploee_edit', {'id': emploee.id}) }}">edit</a>

    {{ include('emploee/_delete_form.html.twig') }}
{% endblock %}
