<form method="post" action="{{ path('app_emploee_delete', {'id': emploee.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ emploee.id) }}">
    <button class="btn">Delete</button>
</form>
